# techniques.example.txt — sample input for gen-navigator-layer.py
# One ATT&CK identifier per line, optional comment after ':'.
# Blank lines and '#' are ignored. Fill in as you work through the box.
#
#  python3 gen-navigator-layer.py -n "Box name" -f techniques.txt -o box.json

# --- Reconnaissance / Discovery ---
T1046:nmap -p- then -sC -sV on the open ports
T1595.002:nikto + nuclei on the web service

# --- Credential Access ---
T1110.003:password spraying via netexec
T1558.004:AS-REP roasting of an account without pre-auth
T1003.006:DCSync of the domain hashes (secretsdump)

# --- Initial Access ---
T1190:exploit of the public web app -> webshell
T1078:reuse of valid creds on SMB/WinRM

# --- Execution ---
T1059:stabilized bash reverse shell

# --- Privilege Escalation ---
T1548:misconfigured sudo (GTFOBins)
T1068:local exploit to root

# --- Lateral Movement ---
T1550.002:Pass-the-Hash to a 2nd host
T1021.006:WinRM access (evil-winrm)

# --- Command & Control (multi-machine labs) ---
T1090.001:internal proxy via ligolo-ng
T1572:chisel tunnel to the subnet

# --- Collection ---
T1005:retrieval of user.txt and root.txt
